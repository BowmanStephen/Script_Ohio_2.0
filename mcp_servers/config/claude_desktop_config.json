{
  "mcpServers": {
    "database_postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://localhost/college_football"
      ],
      "env": {
        "POSTGRES_PASSWORD": "${POSTGRES_PASSWORD:postgres}"
      },
      "description": "PostgreSQL database integration for college football analytics"
    },
    "database_sqlite": {
      "command": "uvx",
      "args": [
        "mcp-server-sqlite",
        "--database-path",
        "data/football_analysis.db"
      ],
      "description": "SQLite database for local analytics operations"
    },
    "data_processing_pandas": {
      "command": "uvx",
      "args": [
        "mcp-server-pandas"
      ],
      "description": "Advanced data processing with pandas for college football metrics"
    },
    "csv_editor": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-csv-editor"
      ],
      "description": "CSV editing and processing for game data and statistics"
    },
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/Users/stephen_bowman/Documents/GitHub/Script_Ohio_2.0"
      ],
      "description": "File system access for managing analytics files and notebooks"
    },
    "memory": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-memory"
      ],
      "description": "Persistent memory for storing analytics insights and context"
    },
    "web_fetch": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-fetch"
      ],
      "description": "Web content fetching for additional sports data sources"
    },
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub integration for version control and collaboration"
    },
    "visualization_echarts": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-echarts"
      ],
      "description": "ECharts integration for dynamic football visualizations"
    },
    "datawrapper": {
      "command": "uvx",
      "args": [
        "mcp-server-datawrapper"
      ],
      "description": "Datawrapper integration for professional chart creation"
    },
    "quickchart": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-quickchart"
      ],
      "description": "QuickChart API for real-time chart generation"
    }
  },
  "systemPrompts": [
    "You are an expert college football analyst with access to advanced analytics tools.",
    "When analyzing football data, always consider context like opponent strength, injuries, and weather conditions.",
    "Use the database tools to query historical performance data and current season statistics.",
    "Leverage visualization tools to create insightful charts and dashboards.",
    "Apply data processing techniques to clean and prepare data for analysis.",
    "Store important insights in memory for future reference and context building."
  ]
}