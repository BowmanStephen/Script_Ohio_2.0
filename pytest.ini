[pytest]
markers =
    integration: integration-level tests that exercise multiple agents
    performance: lightweight performance benchmarks for orchestrator flows
addopts = --strict-markers
testpaths =
    tests
    agents/tests
